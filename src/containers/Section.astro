---
interface Props {
  title?: string;
  className?: string;
}

const { title, className } = Astro.props;
---

<section class={`max-w-4xl mx-auto mb-12 px-4 ${className}`}>
  {title && <h2 class="mb-2 font-bold text-2xl leading-normal">{title}</h2>}
  <slot />
</section>
